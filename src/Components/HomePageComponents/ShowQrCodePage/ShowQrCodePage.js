import React from 'react';
import QRCode from 'react-qr-code';

const ShowQrCodePage = ({qrCodeValue,showQrCode}) => {
    console.log('test',showQrCode,qrCodeValue);
    return (
        <div>
            hello from qr code
            
        </div>
    );
};

export default ShowQrCodePage;